<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.pollingtest">
    <!-- permissions for internet -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FirebaseCrudApp">
        <activity
            android:name=".MainActivity"
            android:exported="false" />
        <activity
            android:name=".Login.ChooseHouseActivity"
            android:exported="false" />
        <activity
            android:name=".GroceryList.GroceryActivity"
            android:exported="false" />
        <activity
            android:name=".Login.RegistrationActivity"
            android:exported="false" />
        <activity
            android:name=".Polls.EditPollActivity"
            android:label="Edit Poll" />
        <activity
            android:name=".Polls.VotingActivity"
            android:label="Vote" />
        <activity
            android:name=".Polls.AddPollActivity"
            android:label="Add Poll" /> <!-- login activity is set as launcher activity -->
        <activity
            android:name=".Login.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Polls.PollActivity" />
    </application>

</manifest>